The dictionary librispeech_phn_reduced_dict.txt is generated by removing the phoneme stress markers in the original librispeech lexicon. Specific 
command to generate this files is below:

$ perl -nae '$w = shift @F; if(! $seen{$w}) {$seen{$w} = 1; $prn=join(" ", @F); $prn =~ s/(\D)\d(\s*)/$1$2/g; print "$w $prn\n";}' librispeech-lexicon.txt
